# MindMic Web Extension
The fully isolated web module actively securely injecting microphone payloads natively parsing chunks into base-layer arrays crossing out over HTTP networks securely bound.

## How It Works
Because secure browser engines forcefully sandbox rendering and system properties, this extension completely bypasses internal boundaries recursively executing an invisible `background.js` worker and dynamic `offscreen.js` portal.

The root script natively intercepts all `.env` constants seamlessly hydrating the Web Extension dynamically without actively triggering security flags locking out file fetching protocols dynamically processing inside specific floating inputs rendering directly on-page against active text inputs globally.

## Usage
Simply load the local `MindMic_Web_Extension` array actively inside any Chrome-derivative browser via standard Developer Flags mapping:
1. Load Chrome -> `chrome://extensions/`
2. Enable Developer Toggle
3. Click "Load Unpacked" pointing directly against this folder layout!
