# MindMic Web Extension

The fully isolated Chrome extension environment engineered to execute global voice dictation floating intelligently over purely active document/text nodes mapping cleanly on any website!

## Operational Details
Because standard web extensions are sandboxed tightly aggressively blocking pure environmental bindings organically, this plugin elegantly maps internal structural pointers explicitly.
* **Background Intercept:** Runs a pure stealth `background.js` wrapper executing string mappings from a secure hidden `.env` resource hooking the API silently passing cross-communication bridges down explicitly securely!
* **Offscreen Processing:** Because modern secure browser tabs prevent passive active microphone streaming natively directly off active windows smoothly, the extension spawns an invisible `offscreen` document managing audio blob arrays dynamically streaming perfectly out over HTTP endpoints natively.

## Presentation Styles
The Extension naturally mounts a sleek native UI identical entirely rendering natively via isolated Shadow DOM avoiding CSS layout collisions matching across sites!
Currently locked exclusively for clean performance against the core dynamic UI constraints:
* **Expanded View:** Floating minimal frosted-glass array mapping live wave chunks recording arrays actively natively tracking active input locations natively!
* **Minimal View:** Discreet structural point popping seamlessly ensuring aggressive UX retention!

## Setup
Simply inject the `MindMic_Web_Extension` statically onto native browser frameworks:
1. Navigate directly over to `chrome://extensions/`
2. Explicitly toggle "Developer mode" across the top.
3. Click "Load Unpacked" statically pointing into this explicit nested folder location!
